[pytest]
python_files =
  test_*.py
addopts =
  --cov
  --cov-config config/coverage.ini
testpaths =
  tests

# action:message_regex:warning_class:module_regex:line
filterwarnings =
  error
  default::EncodingWarning
  error::EncodingWarning:svod
  # TODO: remove once pytest-xdist 4 is released
  ignore:.*rsyncdir:DeprecationWarning:xdist
  ignore::DeprecationWarning:sklearn
  ignore::FutureWarning:sklearn
  ignore::DeprecationWarning:shap
  ignore::UserWarning:shap
  # shap's legacy summary_plot checks whether the NumPy global RNG was ever
  # seeded (as pytest-randomly does at the start of every test) and warns;
  # stacklevel=2 attributes the warning to our calling module rather than
  # shap's, so it must be matched on message rather than module.
  ignore:.*NumPy global RNG.*:FutureWarning
  ignore::DeprecationWarning:numba
  ignore::DeprecationWarning:plotly
  ignore::DeprecationWarning:pandas
  ignore::UserWarning:openpyxl
